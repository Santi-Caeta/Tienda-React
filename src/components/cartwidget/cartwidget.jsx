import {Badge, Avatar} from 'antd';

const Cartwidget = () => {
    return (
        <Badge count={7}>
            <Avatar shape="square" size="large" />
        </Badge>
    )
}

export default Cartwidget;